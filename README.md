# intelligenza

An AI powered python compiler.

# Todo

- [] Add the ability to select which OpenAI model the user wants to use (dont forget to give the user the option to change the thinking level of the model for reasoning models you should do this the same why open ai does classifying them as diffrent models)
- [x] Add the ability to select if the user wants to save the .s file or not. make saving the default behavior (this is harder than it seems as the program has to save to execute so what really should happen is the program deletes the file it made after executing) -Hadi
- [] Make it so the saved .s file has the same name as the inputed python file (again harder than it seems as the user passes in the file with the .py extension so you have to strip that also you have to update the execute logic too)
- [] Make a (Bash or Python) script that benchmarks how fast a command takes to run (ie. how fast python main.py took to run vs gcc gcc -o main main.s) -Caleb
- [] Once everyone is done, upload to pyi -Hadi
- [] record demo vid -Team
