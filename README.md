# intelligenza

An AI powered python compiler.

# Todo

- [] Add the ablity to select which open ai model the user wants to use
- [] Add the ablity to select if the user wants to save the .s file or not. make saving the default behavior (this is harder than it seems as the program has to save to excute so what really should happen is the program deletes the file it made after executing)
- [] Make it so the saved .s file has the same name as the inputed python file
- [] Make a bash script that benchmarks how fast a command takes to run (ie. how fast python main.py took to run vs gcc ) - Hadi
